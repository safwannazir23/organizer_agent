services:
  - type: web
    name: ai-organizer-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false # User will set this in Render dashboard
      - key: PORT
        value: 5000
